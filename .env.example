# Cardibih - FREE Document Extraction Setup
# No API keys needed! Uses Tesseract OCR + Regex patterns

# Optional: If you want to use OpenAI Vision API for better OCR accuracy (not required)
# OPENAI_API_KEY=sk-...

# That's it! The system uses completely free tools:
# - Tesseract OCR (local, free)
# - Regex patterns (free, no API calls)
